#include "main.h"

/**
 * _isupper - check is uppervase
 * @c:the char to check
 * Description: chec if char is upper
 * Return: 1 is uppercase
 */

int _isupper(int c)
{
	char alphabet;

	for (alphabet = 'A'; alphabet <= 'Z'; alphabet++)
	{
		if (c == alphabet)
		}
			return (1);
		}
	}
	return (0);
}
